services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ecom_monolith_java
      POSTGRES_USER: ecom
      POSTGRES_PASSWORD: ecom
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
